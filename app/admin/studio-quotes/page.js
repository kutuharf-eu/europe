import AdminStudioQuotesClient from '@/components/AdminStudioQuotesClient';

export const metadata = {
  title: 'Stüdyo Teklifleri — KUTUHARF Admin',
  robots: { index: false, follow: false },
};

export default function AdminStudioQuotesPage() {
  return <AdminStudioQuotesClient />;
}
